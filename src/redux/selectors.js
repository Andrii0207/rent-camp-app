export const selectAll = state => state.adverts.all;

export const selectAdverts = state => state.adverts.entity;

export const selectIsLoading = state => state.adverts.isLoading;

export const selectOptions = state => state.adverts.options;

export const selectFavorites = state => state.favorites.favoriteList;